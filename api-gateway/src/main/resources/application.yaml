server:
    port: 8888

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: notification_service
          uri: http://localhost:8082
          predicates:
          - Path=${app.api-prefix}/notification/**
          filters:
          - StripPrefix=2



